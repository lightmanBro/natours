{
          "configurations": [
                    {
                              "type": "node-terminal",
                              "name": "Run Script: compile:sass",
                              "request": "launch",
                              "command": "npm run compile:sass",
                              "cwd": "${workspaceFolder}"
                    }
          ]
}